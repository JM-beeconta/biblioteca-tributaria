name: Cancelar actualización en curso

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cancel-update.yml'

concurrency:
  group: biblioteca-tributaria
  cancel-in-progress: true

jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actualización histórica pausada para ejecutar validación controlada."
